const initialState = {

}

export default function appReducer(state = initialState, action){
    switch(action.type) {
        //actions here
        default:
            //return state unchanged
        return state;
    }
}